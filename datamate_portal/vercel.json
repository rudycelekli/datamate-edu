{
  "framework": "nextjs",
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "nodeVersion": "20.x",
  "crons": [
    {
      "path": "/api/mineduc/sync",
      "schedule": "0 2 * * 0"
    }
  ]
}
